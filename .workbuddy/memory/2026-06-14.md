# 2026-06-14

## 修复 CI 报错

从 Next.js 迁移到 Vite + vite-react-ssg 后的遗留问题全部修复：

1. **删除 Next.js 残留**：`src/app/` 目录、`next.config.ts`、`next-env.d.ts`、`.next/` 缓存目录
2. **修复 routes.tsx 导出**：改为 `export default`（之前是 named export，main.tsx 用 default import 导致 TS 报错）
3. **修复 Layout.tsx CSS 路径**：`./globals.css` → `../globals.css`；同时简化 Layout 为只输出 `<Outlet />`
4. **重写 ESLint 配置**：移除 `eslint-config-next`，替换为 `typescript-eslint` + `eslint-plugin-react-hooks` + `eslint-plugin-react-refresh` + `eslint-plugin-oxlint`
5. **修复 oxlint 警告**：`main.tsx` 中 `router` 参数未使用，移除参数声明
6. **更新 .gitignore**：移除 Next.js 项（`.next/`、`out/`、`.vercel`、`next-env.d.ts`），添加 `dist/`
7. **lint 脚本限定范围**：`oxlint src/ && eslint src/`，避免扫描 dist/node_modules 等
8. **新增 typecheck 脚本**：`tsc --noEmit`

验证结果：typecheck ✅ | oxlint ✅ | eslint ✅ | build ✅

## 修复 npm audit 安全漏洞

- 2 个 high severity 漏洞：esbuild 0.17.0-0.28.0 的 GHSA-gv7w-rqvm-qjhr（Deno RCE）和 GHSA-g7r4-m6w7-qqqr（Windows 路径穿越）
- Vite 7.3.5 绑定 esbuild@^0.27.0，实际安装 0.27.7，受影响
- 修复方式：在 package.json 添加 `overrides.esbuild: "^0.28.1"`，强制覆盖为已修复版本 0.28.1
- `npm audit` 结果：0 vulnerabilities ✅
- 所有 CI 验证通过：typecheck ✅ | lint ✅ | build ✅
