# 2026-06-10

## 初始化 Next.js 项目

- 在 `C:\app\yellowhub` 初始化了 Next.js 16.2.9 + TypeScript 项目
- 配置：App Router、ESLint、src/ 目录、import alias `@/*`、无 Tailwind
- 由于目录非空，采用临时目录创建后复制文件的方式完成初始化
- 添加了 `.gitignore`（标准 Next.js 模板 + `.env` 文件）
- package.json 项目名已修正为 `yellowhub`

## 添加 oxlint

- 安装 `oxlint` + `eslint-plugin-oxlint` (v1.69.0)
- `eslint.config.mjs` 加入 `oxlint.configs["flat/recommended"]`，自动关闭 ESLint 中与 oxlint 重叠的规则
- `package.json` scripts 更新：`lint` = oxlint 先跑、再跑 ESLint；`lint:ox` 单独跑 oxlint；`lint:eslint` 单独跑 ESLint
